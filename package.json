{
  "name": "travel-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run build:copy",
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/css/output.css --minify",
    "build:copy": "mkdir dist 2>nul || true && xcopy /E /I /Y src\\*.html dist\\ && xcopy /E /I /Y img dist\\img\\ && xcopy /E /I /Y video dist\\video\\",
    "dev": "tailwindcss -i ./src/input.css -o ./dist/css/output.css --watch"
  },
  "dependencies": {
    "tailwindcss": "^3.3.0"
  }
}}
}
}
