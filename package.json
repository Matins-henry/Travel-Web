{
  "name": "travel-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run copy:html",
    "build:css": "npx tailwindcss -i src/input.css -o dist/output.css --minify",
    "copy:html": "mkdir -p dist && cp -r src/* dist/ && cp -r img dist/ && cp -r video dist/",
    "dev": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
