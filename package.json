{
  "name": "travel-web",
  "version": "1.0.0",
  "private": false,
  "description": "A travel-themed web project using Tailwind CSS",
  "author": "Matins",
  "scripts": {
    "dev": "concurrently \"npm run watch:css\" \"npm run copy:assets:dev\"",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/css/output.css --watch",
    "build": "chmod +x build.sh && ./build.sh",
    "copy:assets:dev": "mkdir -p dist && cp -r src/*.html dist/ && cp -r img dist/ && cp -r video dist/"
  },
  "dependencies": {
    "tailwindcss": "^3.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
}
